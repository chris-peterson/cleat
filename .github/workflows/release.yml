name: Release

# Delegates to shipyard's reusable release workflow. secrets: inherit passes
# MARKETPLACE_DISPATCH_TOKEN through.

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  release:
    uses: chris-peterson/shipyard/.github/workflows/release.yml@v1
    secrets: inherit
